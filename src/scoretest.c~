#include "score.h"
#include <stdio.h>

int main(){
	char a;
	int i;
	for (i=0;i<26;i++) printf("A[%d]=%d\n",AlphabeticalScoreMap[i]);
	/*for (a='A';a<='Z';a++){
		printf("indeks %c di map adalah %d, dengan value %d\n",a,hashfunction(a),getMapVal(a));
	}*/
}
